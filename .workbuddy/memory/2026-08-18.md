# 2026-08-18

## 工作台「新建待办」弹窗 UI 优化 + 提醒功能打磨
- 方向（已与用户确认）：弹窗走单列分区优化；提醒做 UI 打磨 + 实时「下次提醒」预览（不扩展数据模型）。
- `src/workbench-window.js` `buildDialogContent` 改造：
  - 弹窗内容按 `.section` 分区：内容（类型/标题/备注）/ 执行时间 / 参与人 / 地点 / 提醒 / 图片，含 `.section-title` 左侧色条；标题栏分组名改为 `.quad-pill` 彩色药丸；类型图标与「类型」标签同行（`eb-type-row`）。
  - 提醒重构为分段控件（`.rem-modes` 连体按钮，选中态白底绿字），新增 `#rem-preview` 实时预览：基于当前执行时间算「下次执行发生时刻 → 提前提醒时刻」，显示「⏰ 将于 YYYY-MM-DD HH:MM 提醒（提前X）」；执行时间已过则转 `.warn` 红字「提醒时间已过」。
  - 抽取 `readSchedule()` / `readReminder()` / `computeReminderPreview()` / `nextOccurrence()` / `reminderMomentLocal()` 复用函数（submit 与预览共用，避免重复逻辑）。
  - 监听 `overlay` 的 `input`/`change` 事件 + tab/周期类型切换时实时刷新预览；`remOn`/`rem-modes` 点击也刷新。
  - 操作按钮 `.actions` 改为 `position:sticky; bottom:0` 吸底，长表单滚动时始终可见。
- `src/workbench-window.html`：新增上述 class 的 CSS（`.section`/`.section-title`/`.quad-pill`/`.rem-box`/`.rem-toggle`/分段 `.rem-modes`/`.rem-preview`(.warn)/吸底 `.actions`）；`.rem-box` 由内联样式改为 class。
- 校验：`node --check src/workbench-window.js` 通过；独立 node 片段验证预览日期算数（单次+提前1天→前一日09:00；每天+提前5分→次日09:25；禁用→隐藏预览）均正确。预览服务 `python -m http.server 8769`（src/）HTTP 200。
- ⚠️ 改动仅 HTML/JS（前端），无需主进程重启即可在浏览器/打包后见效；地图仍需自备高德 Key。

## 工作台功能升级（UI + 搜索筛选 + 周期间隔 + 自定义提醒 + 快捷键）
- **UI 整体升级** `src/workbench-window.html` + `.js`：顶栏加毛玻璃 + 渐变 logo + 「＋新建」按钮；新增 `.toolbar`（搜索框 / 类型筛选下拉 / 隐藏已完成勾选）；卡片阴影/圆角/微交互打磨，配色变量化（--wb-*）。
- **搜索/筛选/隐藏已完成**：`renderBoard` 增加 `matchFilter()`（标题/备注/参与人模糊匹配 + 类型过滤 + hideDone）；工具栏 input/select/checkbox 实时 `applyToolbar()` 重渲染；无匹配时整屏 `.board-empty` 提示。
- **周期「每隔 X 日/周/月/年 hh:mm」**：`lib/workbenchStore.js` recur 增加 `interval` + `anchorDate`；`occOnDate` 重写支持按 interval 跳过（日按天差、周按周一差/7、月按月差、年按年差，anchorDate 为第0次基准，缺省回退今天）。UI 周期 tab 改为分段「每天/每周/每月/每年」+「每隔[N]次 + 单位 select + 执行时间」，周/月/年条件子块联动显隐；`scheduleLabel` 渲染"每隔X"文案。
- **提醒「提前 X 天 hh:mm」**：reminder 增加 `mode:'custom'`（`leadDays`+`leadTime`）；`reminderMoment` 增加 custom 分支；UI 提醒档增加「提前X天」按钮 → `#rem-custom`（天数 + 时间）；`computeReminderPreview` 文案同步。
- **快捷键（设置菜单可定制）**：`main.js` 新增 `registerWorkbenchHotkey()`（默认 `CommandOrControl+Shift+B`），在 startup / `settings-save`(`workbenchHotkey`) / `hotkey-capture` 中注册/注销；`checkReminders` 增加 custom 文案。**`src/settings-window.html`** 通用面板加「显示/隐藏个人工作台快捷键」捕获输入（复用 `renderHotkeyInput` 模式 → `renderWorkbenchHotkeyInput`，存 `workbenchHotkey`）。
- **修复真 bug**：`getPendingReminders` 原扫描窗口 `now+2d` 太窄，「提前X天」提醒的执行时刻会落在窗口外而永不触发；改为按最大 `leadDays` 扩展 `to = now + maxLead + 1 天`（封顶 400）。已用 node 验证 5 项 custom 场景全过；interval occOnDate 14 项全过。
- 校验：`node --check` main.js / workbenchStore.js / workbench-window.js / preload.js 全过；预览 `http://localhost:8769/workbench-window.html` HTTP 200。
- ⚠️ 本轮动了主进程（main.js 快捷键 + 提醒文案）与存储模型（interval/custom），**需重启 Electron 生效**；地图仍自备高德 Key；快捷键在「设置-通用」中可改并即时生效。

## 工作台 UI Mac 风格紧凑化
- 仅改 `src/workbench-window.html`（CSS，无 JS 逻辑变更）。按"发丝级边框 / 收紧边距 / 收窄辅助区"三目标：
  - 边框：新增更浅发丝色 `--wb-line:#ECEFEE` 替代旧 `--wb-line:#E6EDEA`；并把残留硬码 `#D8E5DF`/`#E4EBE8` 边框统一改为 `var(--wb-line)`；阴影 `--wb-shadow` 改更轻（`0 1px 3px + 0 4px 14px`）。
  - 边距：顶栏 58→46px / 内距 22→16；工具栏 10×22→7×16；主区 padding/gap 16→12；卡片/分区/弹窗内距整体收窄（quad 14/16→11/13、task 9/11→7/9、dialog 18→16、card 18→13、section 16/14→12/11 等）；圆角整体 14-16→9-12，控件更小（搜索框/select/按钮/分段控件字号 13→12.5、checkbox 17→16、task-img 44→40）。
  - 辅助区：侧栏 `.sidebar` 宽 360→280px（时钟+日历整体显示面积变小）；`clock-time` 52→38px；日历 nav 按钮 26→22、格子字号 13→12.5；收起把手 `.collapse-handle` right 376→292px（与 280 宽对齐）。
- 校验：预览 `http://localhost:8769/workbench-window.html` HTTP 200；仅 CSS 改动，浏览器/打包均即时生效，无需主进程重启（对比上一轮）。
