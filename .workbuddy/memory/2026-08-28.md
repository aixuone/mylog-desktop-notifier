# 2026-08-28 工作日志

## AppHeader 设计稿 v2（design/app-header-design-v2.html）
- 用户反馈上版 AppHeader 问题：①功能未做全集 ②UI 需重新设计去重 ③搜索框要正中 ④右侧为 Electron 注入的窗口按钮留空间。要求先出设计稿（html+css+js 无实际功能）审核后再开发。
- 产出 `desktop-notifier/design/app-header-design-v2.html`：静态可交互设计稿（含演示控制条：环境 浏览器/Electron、主题 亮/暗、状态 在线/重连/被踢、徽标开关）。
- **功能全集补齐**：头像下拉菜单新增 铃声设置/特别关注/下载中心/调试窗口（桌面端项，带环境徽标）；离线/被踢文字提示（"离线·正在重连"/"被踢下线·请重新登录"）头像旁显示。
- **去重决策**：命令面板入口按钮→并入搜索框（点击/Ctrl K 打开命令面板）；StatusIndicator 文本→并入头像状态点（在线绿/离线红/重连橙闪/被踢红）+ 离线文字提示。
- **布局**：左区 品牌+快捷操作×6（通话/会议/聊天/计划/日志/审批角标）；中区 搜索框 `position:absolute; left:50%; translateX(-50%)` 严格居中（验证 offset=0）；右区 消息中心+头像+窗口按钮预留区（浏览器模式虚线占位/Electron 模式由主进程注入，3×44px）。
- **菜单分组**：我的（通用设置/设备检测/修改密码）、通知（铃声设置/特别关注）、下载与调试（下载中心/下载Windows通知程序/调试窗口）、退出登录。
- Playwright DOM 断言验证通过：搜索框居中 offset=0、Electron 切换、菜单展开、状态切换、暗色主题。
- 截图存 `design/_shots/*.png`。
- 待用户审核后开发为 AppHeader.vue 正式版。

## 设计稿 v2 修订（同日，用户反馈后）
- **①头像移到最左 + 显示用户名**：左区顺序变为 头像(含用户名"乔伊" + 状态点 + 下拉) → 品牌(logo/名称/版本) → 分隔线 → 快捷操作×6；离线提示随头像移至左区。
- **②设备出错提醒**：右区新增 麦克风/摄像头/扬声器 三个警示图标（`.ah-dev`，data-dev=mic/cam/spk），正常隐藏，出错时红色图标+角标"!"，点击打开音视频设备检测；演示控制条加 3 个设备错误 checkbox。
- 删除重复项补充：旧 top-bar "头像+姓名+手机号常驻" 精简为 头像+用户名（手机号入菜单头部）。
- DOM 断言通过：搜索框仍居中 offset=0、头像 left=14 最左、用户名显示、设备图标默认全隐藏/全开全显。
- 截图更新至 design/_shots/（1~5 + 6-device-error）。注：目录中出现 v3-*.png（10:13，非本会话生成，疑似用户并行测试）。

## AppHeader 设计稿 v3（用户两条新反馈）
- ①**头像挪到最左 + 显示用户名**：左区顺序 = 头像(状态点)+用户名+chevron ｜ 离线提示 ｜ 品牌 ｜ 快捷操作×6；菜单改左缘展开（验证 menuLeftGap=0）；header 最左 8px。
- ②**新增 麦克风/摄像头/扬声器 异常提醒**：右区消息中心左侧，正常隐藏；任一设备异常显示红色警告图标+红点，点击浮层逐设备列错误原因（未找到可用设备/权限被拒绝或设备被占用/播放设备异常）+「去设备检测」「忽略」。数据源待定（WebRTC 设备获取失败 / TUICallKit 设备状态事件）。
- 产出 `design/app-header-design-v3.html`；Playwright 验证：头像最左(userAtLeft=8)、用户名可见、搜索框仍居中(offset=0)、设备提醒显隐、浮层/菜单展开、暗色+多设备异常3项。
- 截图 `design/_shots/v3-*.png`。

## 二次反馈落实（v2/v3 同步修改）
- 用户反馈 3 点：①快捷操作改"快捷操作区"悬停展开 ②隐藏系统 logo 和版本 ③设备叹号位置错→须压麦克风等图标右上角。
  - 快捷操作区：默认 4 个（拨打语音/发起会议/创建计划/创建执行）+ ⋯ 指示，hover 整组宽度过渡展开 发起聊天/审批（.qa-extra 折叠）；"创建执行"替代原"创建日志"（业务指向待确认）。
  - 品牌：.ah-logo/.ah-brand-ver display:none，仅留"e日志"名称。
  - 设备叹号：.ah-dev/.ah-device 加 position:relative，.dev-x absolute top:0 right:0 + translate(30%,-30%) 压图标右上角。
- v3 设备区重构：单按钮+红点+浮层 → **3 个独立图标（mic/cam/spk）+ 各自右上角叹号**，点击图标打开浮层（列 3 设备错误详情 + 去设备检测/忽略）；JS setDevWarn 按 dev 单选(mic/cam/spk/multi) 控制对应图标显隐。
- **v3 CSS 优先级坑**：`body.electron .ah-device{display:none}`(0,2,1) 会覆盖 `.ah-device.warn{display:flex}`(0,2,0) → 设备图标永不显示；已删多余规则。
- 验证全过（v2/v3）：默认 4显/2折叠/⋯显、hover 6全开、logo/版本隐藏、名称保留、搜索框居中 offset=0、mic 图标 warn 显示 + xOnCorner=true。
- 截图：v2-devices/v2-qa-expanded/v3-devices/v3-qa-expanded/v3-device-pop.png。

## AppHeader.vue 正式版重构（按设计稿 v3，去审批角标）
- 用户确认"按这个改，项目中的头部。没有审批角标"→ 重构 `web/src/components/AppHeader.vue`：
  - 布局：左区 头像(最左+用户名+状态点+离线提示)+应用名"e日志"(logo/版本隐藏)+分隔线+快捷操作区(默认4：拨打语音/会议/计划/执行，hover展开 聊天/审批，**无角标**)；中区 搜索框 absolute 居中(命令面板/Ctrl K)；右区 设备提醒(3图标+右上角叹号+浮层 去设备检测/忽略)+消息中心(popover+未读)+窗口控制(Electron 渲染，调 titlebar:win)。
  - 头像菜单全集：我的(通用设置/设备检测/修改密码 WEB)、通知(铃声设置/特别关注 桌面端)、下载与调试(下载中心/调试窗口 桌面端 + 下载Windows通知程序 WEB)、退出登录；桌面端项仅 isElectron 显示，调 electronAPI.titlebarMenu(key)。
  - 设备状态：预留 `mylog:device-status` 事件(detail {mic,cam,spk:boolean})，外部(WebRTC/TUICallKit)派发即显对应图标；浮层列出各设备错误原因。
  - 创建执行：预留 `mylog:open-exec` 事件（业务待确认）。
  - 删除：审批角标(approvalCount/qa-badge/mylog:approval-count 全部移除)、StatusIndicator 引用(组件文件成死文件)。
  - 构建通过：NODE_OPTIONS=16384 npm run build（layout chunk 2,118 kB）。
- 遗留提示：StatusIndicator.vue 死文件；设备状态事件来源待接；创建执行业务指向待确认。

## AppHeader 修复：图标内联 + 样式 1:1 对齐（同日）
- 用户反馈"UI实现与设计没有1:1相符，图标还不显示"。
- **图标问题**：原实现用 `v-html` 注入 SVG 字符串。Playwright 实验证明 v-html+scoped 后代选择器本身可渲染（17×17px 正常），但为彻底排除环境因素，全部改为**模板内联 SVG**（20 个 svg，v-if/v-else 按 action/key 分发）——内联元素必有 data-v、scoped 直命中，零不确定性。删除 ICONS 常量和 deviceList 的 icon 字段。
- **1:1 样式对齐 v3 设计稿**：.ah-left gap 6px+max-width 56%+min-width 0；.ah-user gap 7px、padding 3px 10px 3px 3px、flex-shrink 0；.ah-avatar 30×30/font 13px；.ah-status bottom 1px；.ah-user-chev 12×12；.ah-appname padding 4px 8px 4px 4px；.ah-qa-group flex-shrink 0；媒体查询对齐（<1000px 隐藏应用名+快捷操作+搜索 210px；<760px 隐藏用户名/箭头/离线提示）。
- 构建通过（3m 18s）。注意：<1000px 窗口下快捷操作自动隐藏是设计行为。

## 图标/贴顶问题根因定位（同日 15:54 反馈）
- 用户仍反馈"贴顶+图标不显示"。**排查结论：代码本身正确，线上部署的是旧版本**。
- 验证：构建产物提取 AppHeader scoped 样式（124 条全部正确编译）+ 合成渲染页实测——快捷操作图标 17×17px 正常显示、应用名/快捷操作区垂直居中正常。
- **根因**：`dist/mylog-pc_v2.7.19.8`（生产版，14:24 构建）**缺 assets 目录**（index.html 引用 /mylog-pc/assets/*.js 但本地无此目录）→ 部署上传不完整 → 线上保留旧 JS → 用户看到旧版 header。
- **修复**：重新 `npm run build`（16GB，2m 58s），生产版 assets 完整（63 文件）。
- ⚠️ **部署注意**：部署必须上传完整 `dist/mylog-pc_v2.7.19.8/`（含 assets），否则线上仍是旧版；14:24 assets 缺失原因（构建被清理/上传流程）需核查。
- 可复用验证法：构建产物提取 scoped 规则 → 静态 HTML + Playwright 断言（图标尺寸/垂直居中/右侧布局）。
