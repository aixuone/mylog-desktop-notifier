# 2026-08-27 工作日志

## web 端新 header 组件（AppHeader.vue）
- 新建 `C:\ixuworkspace\mylog-pc\web\src\components\AppHeader.vue`：合并桌面端自绘标题栏功能（头像下拉菜单、搜索 Ctrl K、快捷操作 通话/会议/聊天/创建计划/创建日志/审批角标、在线状态点、窗口控制仅 Electron）与旧 top-bar 功能（品牌 logo、消息中心弹窗+未读徽标、StatusIndicator、用户菜单 设备检测/修改密码/下载通知程序/退出登录）。
- `layout.vue`：旧 top-bar 模板/状态/样式全部移除，替换为 `<AppHeader @join-meeting @dismiss @view-comment @change-password @logout />`；消息中心数据初始化（notificationStore.init / loadFollowApplies / loadCommentUnread 10s 轮询）与 palette:* 事件监听迁移到 AppHeader；DeviceTestModal 内置组件。
- 审批角标：监听 `mylog:approval-count` 自定义事件（默认 0 隐藏）。
- 窗口控制按钮：`window.electronAPI?.titlebarWin` 存在时显示（仅桌面端）。

## desktop-notifier 删除所有注入 CSS
- `main.js` 删除：TITLEBAR_CSS 常量、injectTitlebar()、_titlebarCssReady、hide/showInpageTitlebar、TITLEBAR_H、did-finish-load/did-navigate-in-page/全屏事件绑定。`titlebar-inpage.js` 不再被引用（死文件，未删除）。
- **保留**：titlebar:win / titlebar:quick-action / titlebar:menu / titlebar:drag IPC（titlebar:win 被 AppHeader 窗口控制按钮使用）；pushTitlebarStatus/pushTitlebarUser（托盘状态推送仍在调用）；buildWindowMenu（在线用户/设置原生菜单）。
- main.js 语法检查通过；web 端 `NODE_OPTIONS=--max-old-space-size=16384 npm run build` 构建成功（dist/mylog-pc_v2.7.19.8）。

## 已知遗留
- 桌面端主窗口 frame:false 无边框 + 无自绘标题栏 → 窗口**无法拖动**（AppHeader 仅有 min/max/close 按钮）。待决策：改 frame:true 或 AppHeader 加 -webkit-app-region:drag 区域。
- `src/titlebar-inpage.js` 为死文件，待确认删除。
- web 端构建需 ≥16GB Node 内存（Vue+TRTC+TUIKit 大包体压缩 OOM）。
